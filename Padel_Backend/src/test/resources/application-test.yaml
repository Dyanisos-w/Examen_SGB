spring:
  datasource:
    url: jdbc:h2:mem:padeltest;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

app:
  datasource:
    user:
      url: jdbc:h2:mem:padeltest;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
      jdbc-url: jdbc:h2:mem:padeltest;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
      driver-class-name: org.h2.Driver
      username: sa
      password:
    admin:
      url: jdbc:h2:mem:padeltest;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
      jdbc-url: jdbc:h2:mem:padeltest;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
      driver-class-name: org.h2.Driver
      username: sa
      password:

jwt:
  secret: test-secret-key-which-is-long-enough-for-tests-1234567890
  expiration: 3600000
  refresh-expiration: 86400000

